version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3001:3001
    volumes:
      - .:/app
    environment:
      - NODE_ENV=developement
    command: npm run start:dev
