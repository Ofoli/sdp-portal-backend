export const MONGO_DB_HOST = "mongodb://db";
export const STATUSES = {
  SUCCESS: true,
  FAILED: false,
};
export const URLS = {
  base: "/",
  users: "/api/users",
  report: "/api/report",
  auth: "/api/auth",
  notFound: "*",
};
